import { TransactionsClient } from './transactions-client'

export default function TransactionsPage() {
  return (
    <TransactionsClient
      userId=""
      portfolios={[]}
      transactions={[]}
    />
  )
}
